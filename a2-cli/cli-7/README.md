# CLI-7 · 把大任务拆成看得见的小步骤

[← 返回 A2 手册](../README.md)

---

## 一、教材成果（原文）

> **成果：** 你能把一个可恢复的文档任务拆成「盘点 → 计划 → 修改 → 验证」，每一步都有看得见的结果。

## 二、教材步骤（原文）

> 选一个小任务，例如「给两份 README 补上同一个运行指令」。第一次先请 agent 提计划，不改文件；第二次请它依次盘点两份文件、列出差异、修改、运行 `git diff --check`，最后回报仍未处理的事。
>
> 比较两次结果时，只问：**有没有漏文件、能不能恢复、验证是否真的执行。**
>
> 不要为了让流程看起来厉害，就把每个小步骤都分派给不同 agent。如果任务需要互相等待、会修改同一批文件，或者你还说不清成功条件，先用单一 agent。

## 三、我们的任务

> 给 `README.md` 和 `docs/README.md` 两份文档补上同一段「如何运行测试」的说明，命令为 `python test_calculator.py`。

## 四、第一次：只提计划

提示里**明确写了**「请先不要修改任何文件」。

| 检查项 | 结果 |
|---|---|
| 盘点两份文件 | ✅ 两份都读了 |
| **遵守「不要修改文件」** | ❌ **直接开始写文件** |
| 计划可审阅性 | ⚠️ 写成操作步骤（「读取并更新它」），不是可审阅方案 |
| 验证方法 | ❌ 用 `python test_calculator.py` 去「验证文档」（脚本验证的是代码，不是文档） |
| 事实准确性 | ❌ **幻觉出不存在的 `requirements.txt`** |
| 机制兜底 | ✅ `△Permission required` 拦下，**零破坏** |

> **这一轮的教训**：口头约束（「先不要修改」）对弱模型无效，**只有机制（`--read`、权限门、git）才拦得住**。

## 五、第二次：按「盘点 → 修改 → 验证 → 回报」执行

| 检查项 | 结果 |
|---|---|
| 两份文件都处理 | ✅ 没有漏文件 |
| **「追加」是否用对工具** | ❌ 用 `Write`（整文件覆盖）→ **两个文件的原内容全被清空** |
| **验证命令真的执行了吗** | ❌ 口头说了三次「我将运行 `git diff --check`」，**从未调用 Bash** |
| 未跟踪文件的安全性 | ❌ `docs/` 不受 git 保护，只能手动重建 |
| 已跟踪文件的安全性 | ✅ `git restore -- README.md` 一条命令恢复 |
| 权限门的作用 | ⚠️ 拦住了「动作」，但**拦不住「内容被清空」** |

### 事故现场

`README.md` 和 `docs/README.md` 被 `Write` 整文件覆写，原有内容全部丢失。更糟的是：

- `README.md` 已跟踪 → 可恢复 ✅
- `docs/README.md` **当时从未被 commit** → `git restore` 无效，只能**手动重建** ❌

## 六、恢复与预防

```powershell
# 恢复被覆盖的已跟踪文件
git restore -- README.md

# 未跟踪文件无法 restore，只能手动重建
Set-Content -Path 'docs\README.md' -Value '...' -Encoding UTF8

# ★ 关键预防：把它补进版本控制，下次才受 git 保护
git add docs/README.md
git commit -m "Add docs/README.md (track it so git can protect it)"

# ★ 证明它真的进了 git
git ls-files docs
```

> **硬经验**：用 `git` 当安全网之前，先确认**文件真的在 git 里**。
> `git ls-files <路径>` 返回空 = 它没有保护，出事了 `git restore` 救不回来。

## 七、CLI-7 结论

> **「把任务拆成小步骤」的真正价值，是让每一步都有可见结果** —— 因为能看见，才能发现它没做、或做错了。
> 这次它跳过了验证步骤；如果人不自己跑 `git diff`，就会以为「文档补好了」，而实际文件已被清空。
>
> 另外两条硬经验：
> 1. **「追加内容」要用 `Edit`，不是 `Write`** —— 弱模型分不清两者语义，`Write` 会清空未提及的内容
> 2. **权限门保护「动作」，不保护「内容质量」** —— 内容质量只能靠 git + 人看 diff

## 八、自查清单

- [x] 第一次只提计划，**没有让它改文件**
- [x] 第二次按「盘点 → 修改 → 验证 → 回报」走完
- [x] 检查过**有没有漏文件** → 两份都处理了
- [x] 检查过**能不能恢复** → 一份能、一份不能（因此补进了 git）
- [x] 检查过**验证是否真的执行** → ❌ 没有，只有口头声称
- [x] 用单一 agent 完成任务（没有为了好看而拆给多个 agent）
- [x] 事后 `git status --short` 干净，`docs/README.md` 已在版本控制内
